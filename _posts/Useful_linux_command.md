---
layout: post
---
- **check the linux distribution type**

  - ```lsb_release -a```

  - ```cat /etc/os-release```
- **check whether a package is installed in the system**
  -```rpm -q $package```
